name = "soul-os-api"
main = "soul-os-api.js"
compatibility_date = "2024-11-01"

account_id = "31e9e8c6fcda0c10e7a4331a8dfb89fe"

workers_dev = true

# Service Bindings (managed in Cloudflare dashboard):
#   SIDDARTHA - service binding to the siddartha worker

# D1 Databases (managed in Cloudflare dashboard):
#   DB - D1 database for sessions, memory, and traces

# Workers AI (managed in Cloudflare dashboard):
#   AI - Workers AI binding

# Secrets (managed via Cloudflare dashboard or `wrangler secret put`):
#   MASTER_KEY
#   OPENAI_API_KEY
#   OPENAI_BASE_URL
#   ANTHROPIC_API_KEY
