# Root wrangler.toml — kept for reference only.
# Each app has its own wrangler.toml under apps/<name>/wrangler.toml.
# Run deployments via the GitHub Actions workflows in .github/workflows/,
# or locally with: wrangler deploy --config apps/<name>/wrangler.toml
